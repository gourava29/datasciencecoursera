library("ggplot2")
if(!is.null("NEI")){
    NEI<-readRDS("summarySCC_PM25.rds") 
}
if(!is.null("SCC")){
  SCC <- readRDS("Source_Classification_Code.rds")
}
tidyData<-aggregate(Emissions~year,NEI,sum)
png("plot1.png", width = 480, height = 480)
print(qplot(year,Emissions,data = tidyData))
dev.off()
