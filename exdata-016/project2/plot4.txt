library("ggplot2")
if(!is.null("NEI"))
  NEI<-readRDS("summarySCC_PM25.rds") 
if(!is.null("SCC"))
  SCC <- readRDS("Source_Classification_Code.rds")
tidyData<-merge(NEI,SCC[grepl("Coal",SCC$EI.Sector),],by="SCC")
tidyData<-tidyData[grepl("Coal",tidyData$EI.Sector),]
# tidyData<-merge(x=tidyData,y=SCC,by="SCC")
png("plot4.png", width = 800, height = 800)
print(qplot(year,Emissions,data = tidyData))
dev.off()